# 使用 `esp-config` 的 TUI（可选）

如果已安装，你可以从项目目录运行 `esp-config` 来启动该工具，它看起来像这样：

![截图](../assets/esp-config.png)

如果工具无法自动识别，你还可以指定目标芯片和要使用的配置文件。

通过 `esp-generate` 生成的项目不需要这样做。

有关配置文件的组织方式以及如何手动编辑它们的详细信息，请参阅[配置章节](../application-development/configuration.md)。
